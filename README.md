# 

# Testing
```
npm install babel --save-dev
npm install babel-core --save-dev
npm install babel-preset-es2015 mocha chai --save-dev
npm install aws-sdk-mock --save-dev
npm install expect.js --save-dev
```
